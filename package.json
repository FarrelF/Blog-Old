{
    "name": "farrelf-blog",
    "devDependencies": {
        "@semantic-release/changelog": "^3.0.4",
        "@semantic-release/exec": "^3.3.5",
        "@semantic-release/git": "^7.0.16",
        "babel-core": "^6.26.3",
        "babel-preset-es2015": "^6.24.1",
        "browser-sync": "^2.26.7",
        "cz-conventional-changelog": "2.1.0",
        "gulp": "^4.0.2",
        "semantic-release": "^15.13.18"
    },
    "config": {
        "commitizen": {
          "path": "./node_modules/cz-conventional-changelog"
        }
    },

    "license": "AGPL-3.0-or-later",
    "scripts": {
        "build": "git clone https://github.com/FarrelF/FarrelF-Blog.git; cd FarrelF-Blog; python3 -m pip install -r requirements.txt; pelican content -o ../public -s publishconf.py"
    },
    "dependencies": {
        "gulp": "^4.0.2"
    },
    "babel": {
        "presets": [
          "es2015"
        ]
    }
}
