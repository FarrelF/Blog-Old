{
    "scripts": {
        "build": "python3 -m pip install virtualenv; virtualenv pelican-env; source pelican-env/bin/activate; pip install -r requirements.txt; pelican content -o output -s publishconf.py; mv output public"
    }
}