{
    "name": "farrelf-blog",
    "license": "AGPL-3.0-or-later",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/FarrelF/FarrelF-Blog.git"
    },
    "devDependencies": {
        "babel-core": "^6.26.3",
        "babel-preset-es2015": "^6.24.1",
        "browser-sync": "^2.26.7",
        "cz-conventional-changelog": "^3.0.2",
        "gulp": "^4.0.2",
        "mem": "^5.1.1"
    },
    "dependencies": {
        "netlify-cli": "^2.20.1"
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-conventional-changelog"
        }
    },
    "scripts": {
        "build": "./build.sh && mv output publish",
        "invoke-devserver": "invoke rebuild && node node_modules/gulp/bin/gulp.js invoke_build",
        "make-devserver": "make REBUILD=1 html && node node_modules/gulp/bin/gulp.js make_html"
    },
    "babel": {
        "presets": [
            "es2015"
        ]
    }
}
